# Roadmap

## Now

- Run the inaugural Codex/GPT-5.5 2000m benchmark attempt through the Open Scaffold evolve/work-record loop.
- Score each generation with the neutral `graphanov/2000m` conformance suite.
- Publish light comparable results to `graphanov/2000m-results` on a review branch.

## Later

- Add a WASM/browser build for the owner's separate human-feel track if the final generation is playable enough to review.
- Use the results repo as the canonical leaderboard spine; do not create showcase repos for every model by default.
